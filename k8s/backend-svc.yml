apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: task-app
  labels:
    app: task-backend
    tier: backend
spec:
  selector:
    app: task-backend
    tier: backend
  ports:
    - name: http
      port: 5050
      targetPort: 5050
      protocol: TCP
  type: ClusterIP
